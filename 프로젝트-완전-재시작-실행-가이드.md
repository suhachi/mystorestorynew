# 프로젝트 완전 재시작 실행 가이드

**작성일**: 2024년 1월 25일
**상태**: ✅ 삭제 완료, 재시작 준비 완료

---

## ✅ 완료된 작업

### 1. 상위 폴더 정리 완료

- ✅ 가이드 문서 파일 (5개) 삭제 완료
- ✅ `src/` 폴더 삭제 완료
- ✅ `functions/` 폴더 삭제 완료
- ✅ Git 저장소 (`.git`) 삭제 완료

### 2. 숙주 폴더 준비 완료

- ✅ 필수 설정 파일 생성 완료
- ✅ 인코딩 손상 방지 설정 완료
- ✅ 개발 가이드 문서 준비 완료
- ✅ Firebase 관련 파일 준비 완료

---

## 🚀 다음 단계: GitHub 초기화 및 재시작

### Step 1: Git 저장소 초기화

```powershell
# 숙주 폴더로 이동
cd C:\Users\a\MyStoreStory\MY_STORE_STORY\MY_STORE_STORYdesign

# Git 저장소 초기화
git init

# 기본 브랜치 이름 설정
git branch -M main

# Git 상태 확인
git status
```

### Step 2: Git 전역 설정 확인

```powershell
# Git 전역 설정 확인 및 설정
git config --global core.autocrlf false
git config --global core.quotepath false
git config --global i18n.commitencoding utf-8
git config --global i18n.logoutputencoding utf-8
git config --global core.safecrlf true

# 설정 확인
git config --global --list | Select-String "core\|i18n"
```

### Step 3: 첫 커밋 (설정 파일만)

```powershell
# 숙주 폴더에서
cd MY_STORE_STORYdesign

# 필수 설정 파일만 스테이징
git add .gitattributes .editorconfig .gitignore .vscode/

# 첫 커밋
git commit -m "chore: 프로젝트 초기 설정 (인코딩, 에디터 설정)"
```

### Step 4: GitHub 저장소 생성

1. **GitHub 웹사이트 접속**
   - https://github.com 접속
   - 로그인

2. **새 저장소 생성**
   - 우측 상단 "+" 버튼 → "New repository"
   - Repository name: `mystorestory`
   - Description: "MyStoreStory - Firebase 기반 배달 플랫폼"
   - Public 또는 Private 선택
   - ⚠️ **중요**: README, .gitignore, license 추가하지 않기

3. **저장소 생성**
   - "Create repository" 클릭

### Step 5: 원격 저장소 연결

```powershell
# 원격 저장소 추가 (HTTPS)
git remote add origin https://github.com/사용자명/mystorestory.git

# 또는 SSH 사용
git remote add origin git@github.com:사용자명/mystorestory.git

# 원격 저장소 확인
git remote -v
```

### Step 6: 파일 정규화 (선택사항)

```powershell
# 기존 파일 정규화 (CRLF → LF)
git add --renormalize .

# 변경사항 확인
git status

# 정규화 커밋
git commit -m "chore: 파일 인코딩 및 줄바꿈 정규화"
```

### Step 7: 소스 코드 커밋

```powershell
# 모든 파일 스테이징
git add .

# 소스 코드 커밋
git commit -m "feat: 초기 프로젝트 구조 및 소스 코드"
```

### Step 8: GitHub에 푸시

```powershell
# 메인 브랜치 푸시
git push -u origin main
```

---

## 📋 최종 체크리스트

### GitHub 초기화
- [ ] Git 저장소 초기화 (`git init`)
- [ ] Git 전역 설정 확인
- [ ] GitHub 저장소 생성
- [ ] 원격 저장소 연결

### 첫 커밋
- [ ] 설정 파일 커밋
- [ ] 파일 정규화 커밋 (선택사항)
- [ ] 소스 코드 커밋
- [ ] GitHub에 푸시

### 개발 준비
- [ ] 의존성 설치 (`pnpm install`)
- [ ] 개발 서버 실행 테스트 (`pnpm dev`)
- [ ] 빌드 테스트 (`pnpm build`)

---

## 🎯 완료!

### 재시작 준비 완료

- ✅ 상위 폴더 정리 완료
- ✅ 숙주 폴더 준비 완료
- ✅ 필수 설정 파일 생성 완료
- ✅ 인코딩 손상 방지 설정 완료
- ✅ 개발 가이드 문서 준비 완료

### 다음 단계

1. **Git 저장소 초기화** (위 Step 1)
2. **GitHub 저장소 생성 및 연결** (위 Step 4-5)
3. **첫 커밋 및 푸시** (위 Step 3, 7-8)
4. **개발 시작!**

---

**작성일**: 2024년 1월 25일
**상태**: ✅ 재시작 준비 완료
**다음 단계**: Git 저장소 초기화 및 GitHub 연결

