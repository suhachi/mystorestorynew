# 다음 단계 실행 가이드

**작성일**: 2024년 1월 25일  
**프로젝트**: MyStoreStory  
**상태**: 배포 준비 완료, 실행 단계

---

## ✅ 완료된 작업

### 1. Git 전역 설정 ✅
- `core.autocrlf: false` 설정 완료
- `core.quotepath: false` 설정 완료

### 2. 프로젝트 설정 확인 ✅
- 인코딩 설정 완벽
- Firebase 설정 완료
- 배포 문서 완료

---

## 🔄 다음 단계 실행 순서

### 단계 1: 환경 변수 설정 (필수)

#### 1.1 Firebase Console 접속
1. https://console.firebase.google.com 접속
2. 프로젝트 선택: `mystorestory`

#### 1.2 환경 변수 값 확인
1. 프로젝트 설정 (⚙️) > 일반 탭
2. "내 앱" 섹션에서 웹 앱 선택 (없으면 추가)
3. "SDK 설정 및 구성" 섹션에서 다음 값 확인:
   - **apiKey**: `VITE_FIREBASE_API_KEY`
   - **authDomain**: `VITE_FIREBASE_AUTH_DOMAIN`
   - **projectId**: `VITE_FIREBASE_PROJECT_ID`
   - **storageBucket**: `VITE_FIREBASE_STORAGE_BUCKET`
   - **messagingSenderId**: `VITE_FIREBASE_MESSAGING_SENDER_ID`
   - **appId**: `VITE_FIREBASE_APP_ID`
   - **measurementId**: `VITE_FIREBASE_MEASUREMENT_ID` (선택)

#### 1.3 .env.local 파일 생성
프로젝트 루트에 `.env.local` 파일 생성:

```env
# Firebase API Key
VITE_FIREBASE_API_KEY=실제_API_키_값

# Firebase Auth Domain
VITE_FIREBASE_AUTH_DOMAIN=mystorestory.firebaseapp.com

# Firebase Project ID
VITE_FIREBASE_PROJECT_ID=mystorestory

# Firebase Storage Bucket
VITE_FIREBASE_STORAGE_BUCKET=mystorestory.appspot.com

# Firebase Messaging Sender ID
VITE_FIREBASE_MESSAGING_SENDER_ID=102904078280

# Firebase App ID
VITE_FIREBASE_APP_ID=실제_앱_ID_값

# Google Analytics Measurement ID (선택)
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX
```

**참고**: `환경-변수-설정-가이드.md` 파일 참고

---

### 단계 2: 의존성 설치 (필수)

```bash
# 프로젝트 디렉토리로 이동
cd MY_STORE_STORYdesign

# 의존성 설치
pnpm install
```

**예상 소요 시간**: 2-5분

**확인 방법**:
- `node_modules/` 폴더 생성 확인
- 설치 완료 메시지 확인

---

### 단계 3: 빌드 테스트 (필수)

```bash
# 빌드 실행
pnpm build
```

**예상 소요 시간**: 1-3분

**성공 기준**:
- 빌드 오류 없음
- `dist/` 폴더 생성
- `dist/index.html` 파일 존재

**실패 시**:
- `빌드-테스트-가이드.md` 참고
- 환경 변수 확인
- 의존성 재설치

---

### 단계 4: Firebase CLI 설정 (필수)

#### 4.1 Firebase CLI 설치
```bash
# Firebase CLI 설치
npm install -g firebase-tools
# 또는
pnpm add -g firebase-tools
```

#### 4.2 Firebase 로그인
```bash
# Firebase 로그인
firebase login
```

#### 4.3 프로젝트 선택
```bash
# 프로젝트 디렉토리로 이동
cd MY_STORE_STORYdesign

# 프로젝트 선택
firebase use mystorestory

# 현재 프로젝트 확인
firebase use
```

---

### 단계 5: 스테이징 배포 (선택)

#### 5.1 배포 스크립트 사용 (권장)
```bash
# Windows PowerShell 또는 Git Bash에서
./src/scripts/deploy.sh staging
```

#### 5.2 수동 배포
```bash
# 1. 빌드
pnpm build

# 2. Firestore Rules 배포
firebase deploy --only firestore:rules

# 3. Firestore Indexes 배포
firebase deploy --only firestore:indexes

# 4. Cloud Functions 배포
firebase deploy --only functions

# 5. Firebase Hosting 배포
firebase deploy --only hosting
```

**참고**: `스테이징-배포-가이드.md` 파일 참고

---

## 📋 체크리스트

### 사전 준비
- [x] Git 전역 설정 완료
- [x] 인코딩 설정 완료
- [x] Firebase 설정 파일 완료
- [x] 배포 문서 완료

### 필수 작업
- [ ] 환경 변수 설정 (`.env.local` 파일 생성)
- [ ] 의존성 설치 (`pnpm install`)
- [ ] 빌드 테스트 (`pnpm build`)
- [ ] Firebase CLI 설치 및 로그인
- [ ] Firebase 프로젝트 선택

### 배포 작업
- [ ] 스테이징 배포 실행
- [ ] 스모크 테스트 실행
- [ ] 프로덕션 배포 실행 (선택)

---

## ⚠️ 주의사항

### 1. 환경 변수 보안
- `.env.local` 파일은 Git에 커밋하지 마세요
- 실제 API 키는 공개 저장소에 올리면 안 됩니다
- 팀원과 공유할 때는 안전한 방법 사용

### 2. 배포 전 확인
- 빌드 테스트 성공 확인
- Firebase Console 설정 확인
- 환경 변수 설정 확인

### 3. 배포 후 확인
- 배포된 사이트 접속 확인
- 기본 기능 테스트
- Firebase 연동 테스트
- 오류 확인

---

## 🔗 관련 문서

1. `환경-변수-설정-가이드.md` - 환경 변수 설정 방법
2. `Firebase-프로젝트-설정-확인-보고서.md` - Firebase 설정 확인
3. `빌드-테스트-가이드.md` - 빌드 테스트 방법
4. `스테이징-배포-가이드.md` - 스테이징 배포 가이드
5. `배포-준비-작업-최종-보고서.md` - 전체 요약

---

## 🎯 빠른 시작

### 최소 필수 작업 (배포 가능)
1. Firebase Console에서 환경 변수 값 확인
2. `.env.local` 파일 생성 및 값 입력
3. `pnpm install` 실행
4. `pnpm build` 실행
5. Firebase CLI 설치 및 로그인
6. `firebase use mystorestory` 실행
7. 배포 스크립트 실행 또는 수동 배포

**예상 소요 시간**: 30분-1시간

---

**작성일**: 2024년 1월 25일  
**상태**: ✅ 실행 가이드 작성 완료  
**다음 단계**: 사용자 작업 진행

